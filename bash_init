xrdb -merge ~/.Xresources

# PATH=$PATH:/usr/local/sbin:/usr/sbin:/sbin:~/bin
PATH=~/.local/bin:$PATH:~/bin:~/.cargo/bin

# finally, start cron if needed
# _discarded=$(pgrep cron)
# if [ ! $? -eq 0 ]; then
#     sudo service cron start
# fi
